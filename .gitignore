# 환경 변수 파일
.env
.env.local
.env.production

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv/
pip-log.txt
pip-delete-this-directory.txt

# Node.js (미래에 사용할 수 있음)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# 로그 파일
*.log
logs/

# 런타임 데이터
pids/
*.pid
*.seed
*.pid.lock

# 임시 파일
.tmp/
temp/

# 에디터 설정
.vscode/
.idea/
*.swp
*.swo

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
nul

# 봇 관련 데이터
bot_data/
*.db
*.sqlite

# API 키 백업
api_keys/
keys/

# 봇 설정 파일 (GCS에만 저장, 로컬 사용 안 함)
run/settings.json
settings.json

# 로컬 백업 데이터 파일 (사용 안 함)
run/web_panel_data.json
run/emoji_map.json
web_panel_data.json
emoji_map.json

# 로컬 백업 (GCS가 주 저장소)
backups/

# 개발/테스트 파일
test_*.py
*_test.py
*.png
*.jpg
*.jpeg

# 대시보드 프론트엔드 에셋은 커밋 허용
!dashboard/frontend/src/assets/**/*.png
!dashboard/frontend/src/assets/**/*.jpg
!dashboard/frontend/src/assets/**/*.jpeg
!dashboard/frontend/src/assets/**/*.svg
!dashboard/frontend/src/assets/**/*.gif
!dashboard/frontend/public/**/*.png



# GCP 관련 (보안상 제외)
google-credentials.json
service-account-key.json
*-key.json
.gcp/

# Webpanel 빌드 파일
webpanel/dist/
webpanel/build/
webpanel/release/
webpanel/.next/
webpanel/out/

# Dashboard frontend 빌드 파일 (로컬 빌드 후 make deploy-dashboard-frontend로 업로드)
dashboard/frontend/dist/

# Electron 빌드 파일
*.asar
*.dmg
*.exe
*.AppImage

# 웹패널 전체 (로컬에만 유지)
webpanel/

# 지원서류 (개인정보)
application/

# 포트폴리오 (개인정보)
portfolio/

# 스크립트 — .py 등 코드는 추적, UI 이미지 자산은 제외
scripts/*.png
scripts/*.jpg

# Claude Code 로컬 전용 설정 (agents/, launch.json 등 공유 자산은 추적)
.claude/settings.local.json

# 봇 로그 파일
bot.log
nohup.out

# Docker 관련
.dockerignore
docker-compose.override.yml

# 캐시 파일
.cache/
*.cache

# Playwright MCP 로그
.playwright-mcp/

# YouTube 쿠키 (민감 데이터)
cookies.txt

# TTS 관련
Debi&Marlene_KOR/
Debi&Marlene_KOR.zip
debi_tts_data.zip

# SFX 음성 파일 (wav만 제외, metadata.csv와 .gitkeep은 포함)
run/assets/sfx/**/*.wav

# TTS 모델 파일 (용량이 큼)
*.pt
*.safetensors
checkpoint-epoch-*/
tests/qwen3_tts_test/output/

# CosyVoice3 전처리 작업 폴더 (음성 파일 + CSV)
tts_training/cosyvoice3/workspace/
cosyvoice3_*_data.zip

# Fish Speech 학습 데이터
tts_training/fish_speech_data/
tts_training/fish_speech_data.zip
tts_training/Fish_Speech_Finetune.ipynb

# CosyVoice3 학습 데이터
tts_training/cosyvoice3_data/
tts_training/cosyvoice3_data.zip
tts_training/style_review.csv
finetune/adapter_qwen3/
finetune/thinker_mlx_4bit/
finetune/__pycache__/
.firecrawl/

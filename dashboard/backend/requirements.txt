flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
requests>=2.31.0
PyJWT>=2.8.0
gunicorn>=21.2.0
google-cloud-storage>=2.14.0
google-cloud-firestore>=2.20.0
Pillow>=10.0.0
aiohttp>=3.9.0
cryptography>=42.0.0
anthropic>=0.40.0
# 2.35.0 이 Firestore database id `(default)` 를 이중 URL 인코딩해
# 모든 호출이 400 Invalid database id %28default%29 로 죽는다.
# 2.34.0 에서는 같은 코드가 정상 동작한다(두 버전 대조 확인).
google-api-core<2.35
